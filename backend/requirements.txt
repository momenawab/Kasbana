Django>=5.0,<5.2
djangorestframework>=3.15
django-cors-headers>=4.4
python-dotenv>=1.0
gunicorn>=22.0
# Database drivers (uncomment the one you use in production):
# mysqlclient>=2.2        # MySQL / MariaDB (Hostinger)
# psycopg[binary]>=3.2    # PostgreSQL
